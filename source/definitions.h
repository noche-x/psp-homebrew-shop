#pragma once

#define VERSION "pre alpha test with connection and i can't finish a project i am doing something wrong"

enum PacketIDS
{
    NONE = 0,
    GET_APPS = 300,
    SEND_APPS = 301,
    GET_FILE = 302,
    SEND_FILE = 303 
};
